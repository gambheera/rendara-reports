{
  "name": "viewer-demo-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/viewer-demo-e2e/src",
  "tags": ["type:e2e"],
  "implicitDependencies": ["viewer-demo"],
  "// e2e": "Playwright's own webServer starts (and waits on) the dev server; clearing the inferred serve dependency avoids double-starting it, which trips Nx's recursive-task guard.",
  "targets": {
    "e2e": {
      "dependsOn": []
    }
  }
}
