/**
 * Skeleton placeholder for `@rendara/report-schema` (E0-S2).
 *
 * The real Template JSON types, JSON Schema, ajv validator and migrations land
 * in Epic 1. For now this exports only the contract version so downstream
 * layers have a legal symbol to import and the module boundaries are exercised.
 */
export const SCHEMA_VERSION = '1.0.0' as const;
